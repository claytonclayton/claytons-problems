
#include "../testlib.h"
#include <bits/stdc++.h>
using namespace std;

const int MIN_N = -10'000, MAX_N = 10'000;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    int x1 = inf.readInt(MIN_N, MAX_N, "x1");
    inf.readSpace();
    int c1 = inf.readInt(MIN_N, MAX_N, "c1");
    inf.readSpace();
    int x2 = inf.readInt(MIN_N, MAX_N, "x2");
    inf.readSpace();
    int c2 = inf.readInt(MIN_N, MAX_N, "c2");
    inf.readEoln();
    inf.readEof();
}
